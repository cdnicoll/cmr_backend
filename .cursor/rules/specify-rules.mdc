# cmr-backend_v2 Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-26

## Active Technologies
- Python 3.11+ + FastAPI, Supabase, Pydantic, pydantic-settings (per starter) (002-foundation)
- Supabase (PostgreSQL) — `resource` table via Supabase client or asyncpg (002-foundation)
- Python 3.11+ + Crawl4AI, Supabase, Modal, asyncpg (per starter) (003-scraping)
- Supabase (PostgreSQL) — `resources` table; `scraped_content` JSONB column (existing from Phase 1) (003-scraping)
- Python 3.11+ + youtube-transcript-api, Supabase, Modal (existing from Phase 2) (003b-scraping-youtube)
- Supabase (PostgreSQL) — `resources` table; `scraped_content` JSONB column (existing) (003b-scraping-youtube)
- Python 3.11+ + PydanticAI, OpenAI (or configured provider), Supabase, Modal, asyncpg (per starter) (004-insights)
- Supabase (PostgreSQL) — `resources` table; `insight` JSONB column (existing from Phase 1) (004-insights)
- Python 3.11 + FastAPI, Supabase (client + asyncpg where needed), Modal, httpx, feedparser (RSS), sitemap parsing (stdlib XML or small lib), YouTube Data API or equivalent for channel videos (006-content-discovery)
- PostgreSQL (Supabase): new `discovery_sources` table; existing `resources` table (with optional `discovery_source_id` already present) (006-content-discovery)

- Python 3.11+ + FastAPI, uvicorn, supabase, modal, pyjwt, cryptography, sqlalchemy, asyncpg, pydantic, pydantic-settings, httpx, python-dotenv (001-starter-application)

## Project Structure

```text
src/
tests/
```

## Commands

cd src && pytest && ruff check .

## Code Style

Python 3.11+: Follow standard conventions

## Recent Changes
- 006-content-discovery: Added Python 3.11 + FastAPI, Supabase (client + asyncpg where needed), Modal, httpx, feedparser (RSS), sitemap parsing (stdlib XML or small lib), YouTube Data API or equivalent for channel videos
- 004-insights: Added Python 3.11+ + PydanticAI, OpenAI (or configured provider), Supabase, Modal, asyncpg (per starter)
- 003b-scraping-youtube: Added Python 3.11+ + youtube-transcript-api, Supabase, Modal (existing from Phase 2)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
